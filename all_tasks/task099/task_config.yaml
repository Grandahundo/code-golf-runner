# Task config for task099
task_id: "task099"

budget:
  max_turns: 10
  max_tokens: 300000
  timeout_seconds: 1200
  turn_timeout_seconds: 600
  max_retries: 2

sandbox_mode: "danger-full-access"

verify:
  command: "python verify.py"
  early_stop: true
