# Code Golf — Task 61

## Goal

Write `solve061.py` that is **SHORTER** than `baseline.py` while passing all tests.

## How to Work

1. Read `baseline.py` — understand what `p(grid)` does
2. Write `solve061.py` with same `p(grid)` → 2D list signature
3. Run `python verify.py solve061.py` to test
4. Goal: file smaller than baseline

## Rules

- Must define `p(grid)` function
- Must pass ALL examples
- Smaller = better
- Do NOT read JSON data files — verify.py handles that
