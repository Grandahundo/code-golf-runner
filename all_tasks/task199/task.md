# Code Golf — Task 199

## Goal

Write `solve199.py` that is **SHORTER** than `baseline.py` while passing all tests.

## How to Work

1. Read `baseline.py` — understand what `p(grid)` does
2. Write `solve199.py` with same `p(grid)` → 2D list signature
3. Run `python verify.py solve199.py` to test
4. Goal: file smaller than baseline

## Rules

- Must define `p(grid)` function
- Must pass ALL examples
- Smaller = better
- Do NOT read JSON data files — verify.py handles that
